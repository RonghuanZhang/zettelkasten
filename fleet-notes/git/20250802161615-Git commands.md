---
"type:": fleet-note
"title:": 20250802161615-Git commands
id:: 20250802161629  # 唯一 ID，基于创建时间确保全局唯一
created:: 2025-08-02T16:16:29  # 创建时间（ISO 格式）
url: 
tags:
  - fleet-note
"processed:": false
"archived:": false
---
```shell

git init

git remote add origin https://github.com/ryan-alexander-zhang/lion-golang.git

git fetch origin

git pull origin main --allow-unrelated-histories

git add .

git commit -m "feat: initial commit"

git push origin main

git rm --cached file

git rm -r --cached dir

git reset --soft HEAD~1

```

# Reference