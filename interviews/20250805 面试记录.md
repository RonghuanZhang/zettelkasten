
后端
* 暴露自己部门数据给其他用户
* 合规数据报表生成
data
* Pipeline 开发
临时写 SQL 拉数据
技术分享，分析线上 bug ，复盘后续如何提升

7411 法案裁掉了部门

业务跨度比较大，怎么考虑的？
* 对很多领域都感兴趣
* 技术都相通
* 技术的广度还是深度？
	* 都有，单机 -> 微服务，大数据
		* 怎么去做解耦？怎么去做扩容？
			* 接口，小程序，报单，股票
			* 客户端 -> O 32? 本来TCP 连接
				* 通过订阅中心去解耦，使用 Kafka
			* 怎么监控的？
				* 消费方计算，推送到监控中心
			* 了解过 Prometheus 做过 Grafana，很浅
				* 同时创建数据质量监控规则，数据告警怎么做的？服务器类似Exporter，吐到消息队列
				* 客户端做了一个 Pipeline 的监控平台，依赖平台也是
					* 推模式还是拉模式？
* Hadoop到BigQuery数据源的切换并保证亿级别数据的100%不丢失
	* 不是迁移，是 ETL 的 JOB，从 Hadoop 拉到 BigQuery
	* 怎么保证一致性的？
		* Daily count 对比，weekly，monthly，只是写 SQL 规则，也是依赖于其他平台
		* 数据质量对比，依赖于其他平台的工具
	* Hadoop -> Oracle，ETL Pipeline，怎么处理异构数据？
		* 遇到小数点很大，格式转换，截断
* Pipeline 面向开发工程师，不是数据分析
	* Load 到 DB 后怎么做的权限控制？也是依赖平台工具

数据平台做过最难的事情是什么？
* 怎么去理解数据
* 没有元数据管理平台吗？为什么要找人问？
	* 还是要理解业务所以要问
* 花了很大精力去理解数据模型
	* 有没有办法优化流程？
		* 拿到需求后去 Confluence Page 找 Dev Notes
		* 去看 Pull Reqeust

这儿我感觉有问题，既然有 Metadata Platform，我一般是看 Metadata，然后联系 table/db owner

感觉了解好像挺浅的，特别依赖于平台能力，问数据基础的
* 什么是 data date，batch date？
	* 数据生产时间和处理时间
	* data lag 过长怎么处理？下游消费不到数据怎么办？
		* SLA，报警，邮件，on call
		* 缩小时间，划分成小块，如果一天的数据，先跑半天
			* 跑半天数据 Load 进去，下游消费了怎么办？共享目录 Donefile 信号去识别 Pipeline 是否跑完

为什么不直接用 Spark？要自己开发

* 分为两个 Zone
	* 核心源数据
	* 分析数据
* 安全性考虑
* 不允许部署 Spark 在核心域，所以自己开发了
* 只允许拼 SQL
* 跨 DB 怎么做的？一开始内存中 Join
	* 数据量大怎么办？
		* 用 RocksDB 落地去做 Join

UDF 怎么注册？怎么执行的？
* 多机 UDF 怎么同步？没做
* Pipeline 指定机器中的 UDF
只用在合规部门用

SQL 解释器怎么做的？
* 解析 SQL 到 Java 代码，只是使用，没有二开
* 把 Java 代码写到树里面，扫树执行，解决代码嵌套执行

RockesDB 这块主导做的。


100 G 数据，URL，4 G，统计重复次数，输出 top 10 重复 URL？
* 把 100 G 拆成小文件，排序，选 Top 10
* 最后进行归并排序

八股文时间：

Redis 缓存了解过吗？有哪些数据结构？
* 忘了
MySQL 索引结构和索引优化？
* 类型不一致的索引失效遇到的多

什么叫索引下推？不知道

我有一个需求，帮我落地，你的流程是什么？
* PM 拉会聊需求
* 评估人日
* 分配，跟进
* 开一个 Confluence Page，需求、表字段是否变动、排期
	* 设计表改动，要 ticket，交给 DBA
* 开发
* 做 UT
* 联调
* 生产测试数据
* 上线 Rollback plan
* DevOps
* 灰度

如果线上出现问题你的解决流程是什么？
* 告警平台
* 看是否是代码问题，有没有上线，rollback
* 先保证平台能用，再去 debug

面试反馈：
主要问了数据开发平台方面的内容，优点是对自己做的业务理解还可以，表达基本能说明白，开发流程规范，但简历内容跟实际做的有一些 GAP，主导的模块没那么多，且比较依赖平台能力，候选人自己主导的内容也不难，做的比较浅，另外例行问了几个八股文回答也一般般。我的结论是比较普通，如果有更好的候选人可以考虑，没有可以二面再挖一挖深度。






项目概要: 基于金融合规业务的不同需求构建针对Venmo APP的数据资产的开发。

通过基于GCP dataproc 来构建不同业务的ETL Pipeline，创建用户个人信息，卡
号信息，交易记录等数据资产。同时创建数据质量监控规则，数据告警等，完成从
Hadoop到BigQuery数据源的切换并保证亿级别数据的100%不丢失。同时解决EDL的
数据质量问题:包括数据和业务需求的不一致，数据丢失，以及通过重建Partion等
提升数据查询效率将每日 ETL 任务运行时间从15小时缩短至6小时等。
技术框架: Spark(Dataproc) + BigQuery + Kafka + GCP + Oracle
项目角色: 代码开发、运维、需求沟通对接等

## 如何保证从Hadoop到BigQuery数据源的切换并保证亿级别数据的100 %不丢失？

怎么直到丢失了哪些数据？

迁移后的数据一致性是如何验证的？

怎么补？

ETL 任务运行时间从15小时缩短至6小时，怎么做的？
任务怎么调度的？执行引擎是什么？ETL 数据源有哪些？目标数据写入到哪里了？如何做的数据权限控制？

## 大数据 ETL 开发框架开发

项目概要: 基于公司HRZ和PAZ之间互不通信以及不同业务线数据库各不相同的痛
点，参考Spark设计框架，通过Java来实现Lite版本的数据处理框架。主要通过
MVEL实现SQL解释器的开发，并基于RocksDB开发高性能高吞吐的底层数据处理
框架来实现跨库的数据查询，去重，UDF开发，Replica等。并实现不同下游的
Sink组件，支持可定制化的数据落盘如:Kafka,Oracle,dropzone以及GCP等。 输
出框架至整个大团队，提供基于该框架的二次开发的能力来构建不同的数据
Pipeline,提高整个pipeline的开发效率70%。
技术框架: RocksDB + Kafka + Spring + SpringBatch + MVEL+ Kryo + H2
项目角色: 架构设计、核心代码开发、JIRA 项目管理、需求沟通对接等

参考 Spark 架构时，您具体借鉴了哪些模块？哪些部分是您决定重新实现的？为什么？
请详细说明 MVEL 在框架中的作用，是做 SQL → AST 翻译，还是 runtime 表达式求值？是否支持 SQL 语法解析/重写？
在 RocksDB 中，您如何实现跨数据库的数据 Join？使用了哪种 Join 策略（如 Hash Join、Broadcast Join）？
您提到实现了自定义的 UDF 支持，这些 UDF 是如何被注册、调度、调用的？是编译期注入还是 runtime 解释？

为提高二次开发效率，您做了哪些文档、抽象、模板封装？是否构建了 SDK 或 Codegen 工具？